import Layout from "../common/Layout";
import MainFirstBlock from "../components/MainFirstBlock";

const Shop = () => {
    return (
        <Layout>
            <MainFirstBlock type="shop"/>
        </Layout>
    );
}

export default Shop;